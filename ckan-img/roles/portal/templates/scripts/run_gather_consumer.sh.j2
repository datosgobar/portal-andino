#!/bin/sh
set -e

CONFIG_FILE="{{ CKAN_CONFIG }}/{{ CKAN_CONFIG_FILE }}"
. {{ CKAN_HOME }}/bin/activate
exec {{ CKAN_HOME }}/bin/paster --plugin=ckanext-harvest harvester gather_consumer --config="$CONFIG_FILE"
